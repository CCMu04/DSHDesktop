/** Host half of the dsh-desktop-ui customization: all behavior lives in the client bundle. */
const name = "dsh-desktop-ui";
const inject = [];
function apply(ctx) {}
export { apply, inject, name };
