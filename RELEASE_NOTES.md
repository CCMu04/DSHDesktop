# DSH Desktop v0.1.0-rc.6.6.5 预览版

自动更新全流程修复与打磨：检测到新版本立即弹窗、下载带进度、一键重启安装；侧栏「更新」按钮随阶段变化；发布说明支持 Markdown；解决此前"有更新但不弹窗、弹窗点不动"等真实环境问题。

## 更新内容

### 自动更新现状

- **检测即弹窗**：安装版启动后检测到新版本，弹窗「发现新版本 vX」直接出现（插件侧判定，走系统代理与本地缓存，秒级响应，不再受网络慢/限流影响）；点「**立即更新**」开始下载，弹窗内显示渐变流动进度条与已下载/总大小；下载完成弹窗「**立即重启安装**」，暂不则退出应用时自动安装。
- **侧栏「更新」按钮**：位于侧边栏底部、设置按钮右侧（flex 横排，设置按钮让出空间，hover 高亮互不干扰）；仅展开侧栏且存在新版本时显示；随阶段显示「更新 → 下载中 x% → 重启安装」；点击打开与自动弹窗同一个弹窗。
- **「下次不再自动提醒」**：勾选后同一版本不再自动弹窗（侧栏按钮保留），出现更新版本后重新提醒。
- **发布说明 Markdown 渲染**：标题、列表、加粗、代码、链接等正常排版，限高滚动，链接交给系统浏览器打开。
- **便携版**：保留启动下载提示与「前往下载」直链，弹窗动作自动降级为打开下载页。

### 本轮修复的问题

- 启动不弹窗：弹窗改由插件直接判定（此前依赖 electron-updater 的慢速检查，部分网络下需数分钟且会被节流跳过，表现为反复重启无弹窗）；
- 弹窗点不动：弹窗改为纯原生 DOM 实现（此前独立 React root 的事件委托在真实环境失效，按钮/遮罩/Escape 全部无响应）；
- 主进程日志不落盘：日志流提升为模块级，自动更新错误可查；
- 启动初始化卡死：页面加载超时兜底（60 秒），不再阻塞自动更新等初始化；
- 缓存误清更新状态、侧栏设置按钮被挤丢等布局与状态问题一并修复。

## 验证

- 自动化测试全部通过（55 项）。
- 真实环境多轮实测：弹窗出现、按钮可用、下载进度、重启安装、不再提醒、侧栏按钮布局均已验证。

## 相关文档

- [README.md](https://github.com/CCMu04/DSHDesktop/blob/main/README.md)：项目介绍、下载安装与构建说明
- [CHANGELOG.md](https://github.com/CCMu04/DSHDesktop/blob/main/CHANGELOG.md)：全部版本变更记录
- [docs/TEMPLATES.md](https://github.com/CCMu04/DSHDesktop/blob/main/docs/TEMPLATES.md)：文档格式模板
